# Copy to .env and fill in. .env is gitignored; .env.example is committed.

# Required ONLY for `waypoint discover` against a live model.
# Everything else -- replay, error handling, escalation -- runs without it.
ANTHROPIC_API_KEY=
WAYPOINT_MODEL=claude-opus-5

# Fixture credentials for the local target app. NOT real credentials.
# Injected at type-time by SecretBroker; never enter a snapshot, prompt, or artifact.
MERIDIAN_USER=operator1
MERIDIAN_PASS=changeme

# Headed browser is required for the live-session handoff demo.
WAYPOINT_HEADED=1

# Target app port (the test harness overrides this with a random free port).
PORT=8080
